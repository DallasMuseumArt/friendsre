# ===================================
#  Form Field Definitions
# ===================================

fields:
    limit:
        label: Number of records
        type: number
        description: The number of records to be processed during each run
